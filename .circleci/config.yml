version: 2.1

jobs:
  test:
    docker:
      - image: cimg/node:21.6.2
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run tests
          command: npm run test:ci

workflows:
  version: 2
  test:
    jobs:
      - test
